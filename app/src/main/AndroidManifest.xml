<?xml version="1.0" encoding="utf-8"?>
<!--En este archivo se declaran los componentes de la aplicacion-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EasyVet">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.EasyVet">
            <!--Se asocia a la clase MainActivity usando el punto-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!--Con esto se indica que es la actividad principal-->
                <category android:name="android.intent.category.LAUNCHER" />
                <!--Y es la que se va a mostrar al momento de ejecutar la aplicacion-->
            </intent-filter>
        </activity>
    </application>

</manifest>